删除所有的：docker-compose -f docker-compose.prod.yml down -v
重建所有的：docker-compose -f docker-compose.prod.yml up -d --build
清空所有的数据：./deploy_prod.sh --fresh 
进入数据库：docker-compose -f docker-compose.prod.yml exec db psql -U postgres
查看所有的日志：docker-compose -f docker-compose.prod.yml logs -f